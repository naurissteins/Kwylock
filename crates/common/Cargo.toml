[package]
name = "kwylock-common"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared models/config/IPC types for Kwylock"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }

